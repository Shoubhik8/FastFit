torch
torchvision
# Pinned for compatibility with torch 2.4.x: newer transformers (5.x) and
# diffusers (>=0.35) import APIs that don't exist / aren't parseable on torch 2.4
# (torch.distributed.tensor.device_mesh, _custom_op PEP-604 signatures).
diffusers==0.32.2
transformers==4.49.0
gradio
accelerate
opencv-python
scikit-image
fvcore
cloudpickle
pycocotools
av
huggingface-hub
pillow
pillow-avif-plugin  # registers an AVIF opener; inventory garments are mostly .avif
onnxruntime
omegaconf
einops
dists-pytorch
matplotlib
clean-fid
torchmetrics
tqdm
prettytable
